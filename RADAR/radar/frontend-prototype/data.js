// Placeholder — overwritten by a real scan via POST /scan.
// Delete this file and replace with the output of generate_data_js.py
// if you want to pre-load a specific scan result.
if (!window.RADAR_DATA) {
  window.RADAR_DATA = null;
}
