# RADAR — Status snapshot
# Source-of-truth pour "où on en est". Co-existe avec BACKLOG.md (NOW/NEXT/LATER) et JOURNAL.md (daily).
# Update manuel quand jalon franchi. Pas de duplication avec BACKLOG (référencer les IDs S0.1, S0.2…).

project:
  name: RADAR
  mission: "Veille concurrentielle VC. URL startup → memo concurrentiel structuré <60s."
  context: "Hackathon Linkup — ship > perfect"
  demo_date: 2026-05-29          # remplir
  cut_line_date: 2026-05-25      # J-3 démo, freeze features
  updated_at: 2026-05-19

# ─────────────────────────────────────────────────────────────
# HEALTH — état global, scan rapide
# ─────────────────────────────────────────────────────────────
health:
  pipeline_e2e:     "✅ E2E run sur indy.fr (52.3s, 2026-05-19) — mais data quality issues sur tail concurrents"
  backend_code:     "✅ 3 phases codées, FastAPI complet, cache + rate limit + kill switch. Synthesize phase manquante."
  frontend_code:    "⚠️ Pages + design-system OK, mais prod ≠ vision prototype (cf G10)"
  tests_e2e:        "⚠️ E2E indy.fr OK structurellement, mais 10/15 concurrents stubs vides + scores radar placeholder"
  perf_target:      "✅ 52.3s sur indy.fr <90s. Bench 4 domains restent 91–193s (à re-mesurer après batched research)"
  deploy:           "❌ 0 setup (Vercel frontend + Railway/Render backend prévus)"
  pre_cache_demo:   "❌ 0/5 boîtes pré-computées (Doctolib, Notion, Slite, Alan, Pennylane). À faire APRÈS fix data layer."
  design_system:    "✅ Source-of-truth tokens.ts + tailwind.preset.ts + 12 specs markdown"
  data_quality:     "❌ Source tracking cassé (sourcesScanned=0), scores radar hardcodés 50/50, dedup incomplet"

# ─────────────────────────────────────────────────────────────
# COMPONENTS — par fichier-clé, état + dernière mesure
# ─────────────────────────────────────────────────────────────
components:

  backend:
    pipeline_understand:
      path: radar/backend/pipeline/understand.py
      status: "✅ Codé, eval Braintrust passe"
      last_bench_s: "11–45s sur 4 domains"
      target_s: 15
    pipeline_discover:
      path: radar/backend/pipeline/discover.py
      status: "⚠️ Codé, mais bottleneck perf"
      last_bench_s: "107–132s sur 4 domains"
      target_s: 20
      issue: "5× trop lent. Investiguer Linkup /tasks batch async vs séquentiel"
    pipeline_enrich:
      path: radar/backend/pipeline/enrich.py
      status: "⚠️ MAX_ENRICH=5 cap, 10/15 concurrents en stubs vides. Refactor batched /research nécessaire."
      last_bench_s: "17–22s sur 4 domains"
      target_s: 60
      issue: "Tail enrichment skipped → features/pricing/capabilities/funding vides. Voir G6."
    pipeline_synthesize:
      path: radar/backend/pipeline/synthesize.py
      status: "❌ Phase manquante. Scores radar hardcodés [50,50,50,50,50,50] dans transform.py:192. Voir G7."
      target_s: 5
    main_api:
      path: radar/backend/main.py
      status: "✅ FastAPI routes + CORS + rate limit + kill switch + cache"
    linkup_client:
      path: radar/backend/clients/linkup_client.py
      status: "✅ Wrapper search + fetch + tasks (batch async) + retry/fallback"
    claude_client:
      path: radar/backend/clients/claude_client.py
      status: "✅ Extraction JSON via claude-sonnet-4-20250514"
    models:
      paths:
        - radar/backend/models/company.py
        - radar/backend/models/competitor.py
        - radar/backend/models/pipeline.py
      status: "✅ Pydantic v2, pattern DataPoint (value + confidence + source_url + extracted_at)"
    utils:
      paths:
        - radar/backend/utils/geocoding.py
        - radar/backend/utils/cache.py
        - radar/backend/utils/dedup.py
      status: "✅ Nominatim (1 req/s), cache JSON fichier, dedup par website"

  frontend:
    entry:
      path: radar/frontend/src/pages/index.tsx
      status: "✅ Form URL + state management + API call"
      tested_e2e: false
    components:
      paths:
        - radar/frontend/src/components/CompanyCard.tsx
        - radar/frontend/src/components/landing/
        - radar/frontend/src/components/loading/OperationsConsole.tsx
        - radar/frontend/src/components/ui/
      status: "✅ Codés, jamais montés en prod-like"
    design_system:
      paths:
        - radar/frontend/src/design-system/tokens.ts
        - radar/frontend/src/design-system/tailwind.preset.ts
        - docs/design-system/  # 12 specs markdown
      status: "✅ Source-of-truth complète. Palantir × Perplexity, dark only"
    types:
      path: radar/frontend/src/types/index.ts
      status: "⚠️ Mirrors manuels des modèles Pydantic — drift risk"

  evals:
    paths:
      - radar/backend/evals/eval_understand.py
      - radar/backend/evals/eval_discover.py
      - radar/backend/evals/eval_enrich.py
      - radar/backend/evals/probe_understand.py
      - radar/backend/evals/bench_phases.py
    status: "✅ Braintrust par phase. Pas de pytest e2e"
    bench_results_csv: radar/backend/evals/bench_results.csv

# ─────────────────────────────────────────────────────────────
# BENCH — dernières mesures par domaine (cible total <90s)
# ─────────────────────────────────────────────────────────────
bench_results:
  - domain: linear.app
    understand_s: 11.99
    discover_s: 107.19
    enrich_s: 17.36
    total_s: 136.54
  - domain: pennylane.com
    understand_s: 11.02
    discover_s: 77.97
    enrich_s: 21.58
    total_s: 110.57
  - domain: mistral.ai
    understand_s: 45.02
    discover_s: 132.07
    enrich_s: 16.52
    total_s: 193.61
  - domain: cal.com
    understand_s: 9.65
    discover_s: 59.36
    enrich_s: 22.72
    total_s: 91.73
  - domain: indy.fr
    understand_s: null            # breakdown phase non capturé pour ce run
    discover_s: null
    enrich_s: null
    total_s: 52.3
    scanned_at: 2026-05-19
    note: "E2E full run. Data quality issues: 5/15 enrichis, scores hardcodés, sourcesScanned=0, duplicate Freebe."

# ─────────────────────────────────────────────────────────────
# NEXT MILESTONES — dérivés de BACKLOG.md (référence IDs)
# ─────────────────────────────────────────────────────────────
next_milestones:
  - id: S0.1
    label: Smoke test Phase 1 (understand)
    status: done
    completed: 2026-05-19
  - id: S0.2
    label: Smoke test Phase 2 (discover)
    status: done
    completed: 2026-05-19
    note: "Bottleneck 107–132s sur autres domains à re-mesurer post-batched-research"
  - id: S0.3
    label: Smoke test Phase 3 (enrich)
    status: done
    completed: 2026-05-19
  - id: S0.4
    label: End-to-end via FastAPI
    status: done
    completed: 2026-05-19
    note: "52.3s sur indy.fr. Data quality issues identifiées (G6-G9)."
  - id: S0.5
    label: End-to-end via frontend
    status: done
    completed: 2026-05-19
    note: "Frontend prod affiche mais ne match pas vision prototype (G10)"
  - id: S0.6
    label: Source tracking propagation (G8)
    blocker: true
    eta: "1–2h"
    rationale: "Coeur narrative démo 'sources tracées, mémo défensible'"
    dod: "sourcesScanned > 20 sur indy.fr. Chaque CompetitorProfile a source_urls non-vide."
  - id: S0.7
    label: Dedup fix (G9)
    eta: "30 min"
    rationale: "Quick win qualité"
    dod: "Pas de duplicate dans competitors[] (vs 2× Freebe actuel)"
  - id: S0.8
    label: Synthesize phase implémentation (G7)
    blocker: true
    eta: "2–3h"
    rationale: "Sans scores réels, radar chart et Compare montrent placeholder"
    dod: "radar.scores distincts par competitor, max-min ≥20 sur ≥2 axes"
  - id: S0.9
    label: Batched /research + output schema Linkup (G6)
    blocker: true
    eta: "3–4h"
    rationale: "Enrichit 15/15 sans exploser budget. Aligné memory radar_linkup_cost_trap."
    dod: "features/pricing/capabilities non-vides pour ≥12/15 concurrents. Coût ≤ 1€/scan."
  - id: S1.0
    label: Re-run E2E sur indy.fr + validation critères acceptation
    eta: "30 min"
  - id: S1.1
    label: Décision UI port prototype vs current Next.js (G10)
    eta: "TBD"
    note: "Discussion produit après data layer fixé"

# ─────────────────────────────────────────────────────────────
# GAPS — trous identifiés
# ─────────────────────────────────────────────────────────────
gaps:
  - id: G1
    label: "Test E2E jamais validé"
    severity: RESOLVED
    resolved_at: 2026-05-19
    fix: "E2E run sur indy.fr OK structurellement. Data quality issues séparées en G6-G9."
  - id: G2
    label: "Discover bottleneck 5× trop lent (107–132s vs 20s cible)"
    severity: HIGH
    fix: "À re-mesurer post-batched-research (S0.9). Pattern calls séquentiels suspecté."
  - id: G3
    label: "Pre-cache démo 0/5"
    severity: HIGH
    fix: "Créer scripts/precompute_demo.py. À faire APRÈS data layer fixé (sinon pre-cache de la merde)."
  - id: G4
    label: "Deploy 0 setup"
    severity: MEDIUM
    fix: "S4.1 Railway + S4.2 Vercel + S4.3 smoke prod (J-2 max)"
  - id: G5
    label: "Types Pydantic ↔ TypeScript sync manuelle"
    severity: LOW
    fix: "Acceptable hackathon. Long terme: codegen depuis Pydantic"
  - id: G6
    label: "Tail enrichment skipped — MAX_ENRICH=5 cap laisse 10/15 stubs vides"
    severity: BLOCKER
    fix: "S0.9 — batched /research avec output schema unique pour 15/15 concurrents"
    detected: 2026-05-19
    evidence: "radar_indy.fr_2026-05-19.json — 10 concurrents avec notable=[], pricing model='X stage', features=[]"
  - id: G7
    label: "Synthesize phase non implémentée — scores radar tous [50,50,50,50,50,50]"
    severity: BLOCKER
    fix: "S0.8 — nouveau module radar/backend/pipeline/synthesize.py avec heuristiques explicites"
    detected: 2026-05-19
    evidence: "transform.py:192 hardcode placeholder. Compare/Radar UI affichera scores identiques."
  - id: G8
    label: "Source tracking cassé — sourcesScanned=0 hardcodé"
    severity: HIGH
    fix: "S0.6 — propager sources[] depuis Linkup client jusqu'à transform + champ source_urls dans CompetitorProfile"
    detected: 2026-05-19
    evidence: "transform.py:213 hardcode 0. Coeur narrative 'sources tracées, mémo défensible' invalidé."
  - id: G9
    label: "Dedup incomplet — duplicate Freebe dans output"
    severity: MEDIUM
    fix: "S0.7 — normaliser URL (strip protocol/www/path) dans dedup.py avant compare. Fuzzy name en 2nd pass si besoin."
    detected: 2026-05-19
    evidence: "radar_indy.fr cache contient 2× freebe.me (id collision)"
  - id: G10
    label: "Frontend prod ≠ vision prototype"
    severity: HIGH
    fix: "Décision produit hors scope cette itération (S1.1). Compare/Timeline/Radar chart absents du Next.js prod. Prototype HTML standalone existe mais pas servi."
    detected: 2026-05-19
    evidence: "frontend-prototype/ a 9 screens, frontend/ a 1 page de cards"

# ─────────────────────────────────────────────────────────────
# CUT-LINE — règle freeze démo
# ─────────────────────────────────────────────────────────────
cut_line:
  rule: "J-3 avant démo = freeze features. Polish + fallbacks + pre-cache uniquement. Toute story non-mergée à J-3 → LATER."
  date: TBD

# ─────────────────────────────────────────────────────────────
# REFERENCES — fichiers source-of-truth
# ─────────────────────────────────────────────────────────────
references:
  architecture: RADAR/CLAUDE/CLAUDE.md
  backlog: radar/BACKLOG.md
  journal: radar/JOURNAL.md
  bench: radar/backend/evals/bench_results.csv
  design_system_specs: docs/design-system/
  design_system_tokens: radar/frontend/src/design-system/tokens.ts
